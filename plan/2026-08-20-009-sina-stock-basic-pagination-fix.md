# 修复新浪股票清单分页

日期：2026-08-20  
状态：已完成并通过真实采集

## 问题

- 2026-08-20启动补采时，新浪股票总数接口返回5547。
- 清单接口即使传入 `num=10000`，实际仅返回100条。
- 完整性校验正确阻止了这100条残缺数据写入数据库。

## 修复内容

- 新浪清单请求改为每页100条串行分页。
- 根据总数计算页数，逐页请求并按页码拼接结果。
- 普通页必须返回100条，最后一页必须返回剩余条数；任意一页数量不符即终止且不写库。
- 全部分页完成后再次校验总数量，原有重复代码、最低数量和异常下降校验保持不变。
- 默认请求间隔从1秒调整为2秒，5547条股票预计请求56个清单页和1次总数接口。
- 每个HTTP请求仍最多尝试2次，不进行并发请求或无限重试。

## 验证

- 自动测试使用固定响应样本，没有再次访问新浪真实接口。
- 覆盖单页、跨页拼接、分页数量不足和异常股票字段。
- Maven完整测试通过，共35个测试，失败0，错误0。
- 修复后已完成当天5547只股票基础信息的真实同步。
