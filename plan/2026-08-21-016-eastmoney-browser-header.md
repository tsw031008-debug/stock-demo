# 东方财富浏览器请求头受控验证

日期：2026-08-21  
状态：已完成单页验证，全量分页待应用重启后验证

## 调整内容

- 将资金流向请求的简略 `Mozilla/5.0` 改为固定、完整的Windows Chrome请求头。
- 保留与资金流向页面一致的Referer，并增加中文 `Accept-Language`。
- 不采用每页随机切换User-Agent，避免同一会话请求特征异常。
- 外部请求失败后的第二次尝试由1秒退避调整为30秒，降低短时间连续访问压力。

## 验证

- 本地模拟测试校验User-Agent和Referer确实随请求发送。
- 仅执行一次真实第一页受控请求，结果为 `rc=0`、总数5552、返回100条。
- 未继续请求后续55页，未写数据库。
- Maven完整测试72个全部通过，失败0、错误0。

## 后续操作

- 当前运行中的应用需要重启后才能加载新请求头和退避策略。
- 单页成功不代表连续56页必然稳定，首次全量任务仍需观察分页日志和最终入库数量。
