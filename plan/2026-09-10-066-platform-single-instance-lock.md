# 平台突破使用单机Java锁

- 用户明确当前只运行一个应用实例，暂不考虑多实例。
- selectStocks改为synchronized串行执行，使用Service内TransactionTemplate完成原有事务，确保提交或回滚完成后才释放Java锁。
- 删除Mapper的lockRun及其SQL；完成时通过completeRun插入或更新成功记录。运行表继续区分成功零入选与尚无成功结果，不再用于提前取得协调锁。
- 普通插入、删除和更新仍会产生数据库自身的事务锁，这是数据库正常行为；本次移除的是任务开始时专门用于并发协调的SQL。
- 结果替换、失败回滚保留旧结果、查询接口和选股条件不变，不修改已发布V17迁移。
- 新增双线程调用及失败后重新执行的集成测试，保留原有跨批次回滚测试。
- 相关测试18项通过；完整Maven回归304项通过，失败0、错误0、跳过0。git diff --check通过，src中无lockRun残留。
