# 股票基础信息数据源切换为新浪

日期：2026-08-20  
状态：已被 `2026-08-20-009-sina-stock-basic-pagination-fix.md` 的受控分页方案替代

## 完成内容

- 删除东方财富股票清单数据源实现，默认且唯一的数据源改为新浪财经。
- 使用新浪 `hs_a` 节点获取全部A股代码和名称，不做代码前缀或市场二次过滤。
- 每次同步先请求一次股票总数，再使用一个大页请求完整股票清单，正常共两次HTTP请求。
- 股票总数与清单数量不一致时整批失败，不自动高频翻页，也不写入不完整数据。
- 保留连接超时、读取超时、请求间隔和最多两次网络失败重试。
- 09:10定时执行、启动后补采、同日幂等判断和批量upsert逻辑保持不变。

## 配置

`stock.basic.source` 调整为新浪接口配置：

- `count-url`：新浪A股总数接口；
- `list-url`：新浪A股清单接口；
- `page-size`：单次清单请求数量，默认10000；
- `connect-timeout`、`read-timeout`、`request-interval`：请求控制参数。

## 验证

- 自动测试使用固定新浪响应样本，没有访问新浪或东方财富真实接口。
- 覆盖新浪非标准JSON字段格式、沪深京样本完整解析、数量不完整拒绝和字段异常拒绝。
- Maven完整测试通过，共31个测试，失败0，错误0。

## 待验证

- 等待下一次每日任务受控执行，确认新浪当前真实响应能在一个大页内返回完整清单。
- 若新浪限制单页数量，任务会安全失败并保持数据库不变，需要另行确认低频分页策略后再调整。
