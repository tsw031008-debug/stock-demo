# 两个选股策略启动补跑

日期：2026-09-11

- 平台突破和强势股回调增加ApplicationReadyEvent启动补跑，上海时间15:15及之后、当天是交易日且策略尚未成功完成时执行。
- 启动监听顺序30/31，在现有股票日线启动补采Order(20)之后；实际数据可用性仍由原Service校验。
- 通过各自Service查询现有完成记录，成功零入选也跳过。不增加数据表和配置。
- 启动补跑与定时触发共用各任务实例锁，事务结束后释放；启动检查或计算失败记录日志，不阻止应用启动。
- 仅补当天，不新增周期重试、不自动补历史日期；原15:15定时执行保留。
- 测试覆盖15:15边界、非交易日、已成功跳过、未完成补跑、检查失败隔离及启动顺序。相关任务测试12项通过，完整Maven测试329项通过（失败0、错误0、跳过0），git diff --check通过。
