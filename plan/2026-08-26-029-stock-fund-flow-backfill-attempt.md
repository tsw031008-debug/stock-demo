# 股票资金流向全量回补启动记录

## 状态

未完成，东方财富历史接口当前主动断开连接，任务已安全停止。

## 本次调整

- 全量回补保持单线程，默认请求间隔由3秒调整为1.5秒。
- 保留按结束日期和股票代码缓存，成功响应可以在下次运行时直接复用。
- 保留普通网络错误最多重试2次，并按60秒、120秒退避。
- 保留HTTP 403或429立即停止，不采用并发请求。
- 请求会话不读取系统代理，避免无效代理造成连接失败。

## 运行范围

- 股票数量：5211只沪深A股。
- 日期范围：2026-03-03至2026-08-25，共最近121个交易日。
- 运行前已有缓存：平安银行 `000001` 1份。
- 运行前数据库：2026-08-25正式资金流向5211条，平安银行历史验证数据120条。

## 受控验证结果

- 禁用系统代理前，`000002` 请求因无效代理连接被提前断开。
- 禁用系统代理后，错误变为东方财富服务端直接断开连接，确认代理因素已经排除。
- 按规则完成首次请求、60秒退避重试和120秒退避重试，3次均收到 `RemoteDisconnected`。
- 任务退出码为1；未产生 `000002` 缓存，未写入残缺数据，也未启动5211只全量循环。
- Python固定样本测试由4个增加为5个，全部通过。

## 续跑方式

数据源恢复后使用以下参数续跑：

```text
.venv\Scripts\python.exe scripts\backfill_stock_fund_flow.py --end-date 2026-08-25 --history-days 121 --request-interval 1.5 --max-retries 2 --retry-backoff 60 --progress-every 20
```

首次全量请求预计约2小时10分钟，网络错误退避时间不计入估算。
