# 功能12统计日期查询简化

- 按用户要求，删除功能12的3000条行情数量门槛及专用Mapper方法、XML语句。
- 复用 selectLatestTradeDate()，以日行情表最新日期作为T；无数据时明确报错，不再按数量回退日期。
- 六日期批量查询、ST过滤、个股缺失处理和两榜排名规则保持原有逻辑。
- 更新Service测试，验证只查询最新日期和六日行情，不额外执行数量查询；保留空表报错验证。
- 本记录替代057记录中的最低行情数量日期选择规则。
- 验证：Service和Mapper定向测试3个通过；mvnw test完整测试250个通过，失败0、错误0、跳过0；src中已无已删除方法的引用。
