# 最近121个交易日资金流向回补

日期：2026-08-25  
状态：脚本和单股验证已完成，全量回补未启动

## 完成内容

- 新增离线脚本 `scripts/backfill_stock_fund_flow.py`，按当前东方财富单股历史接口可返回的最近121个交易日回补沪深A股资金流向。
- 每只股票使用一个请求获取多日记录，金额保持接口原始元单位，价格、涨跌幅和主力、超大单、大单净流入及占比映射到现有 `stock_fund_flow` 表。
- 默认单线程、每1.5秒最多一个请求；普通网络异常最多重试2次并按60秒、120秒退避，HTTP 403或429立即停止，不采用代理、随机请求头或无限重试。
- 原始响应按结束日期和股票代码缓存；任务中断后可从缓存续跑，避免重复请求已经成功的股票。
- 使用 `(stock_code, trade_date)` 批量幂等写入；已有每日正式任务 `EAST_MONEY` 数据保持不变，历史数据标记为 `EAST_MONEY_HISTORY`。
- 所有函数补充用途说明，复杂字段映射、限流停止和正式数据保护逻辑保留明确注释或命名。
- 新增Python缓存文件忽略规则，并在回补依赖中显式固定 `requests` 版本。

## 接口验证结论

- 用户提供的贵州茅台响应包含121条记录，日期范围为2026-03-03至2026-08-25，每条13个字段。
- `lmt=500` 仍只返回121条，当前接口可用范围按最近约121个交易日处理，不宣称支持两年历史。
- 2026-08-25贵州茅台历史响应与数据库每日正式同步记录完全一致，确认金额单位为元，字段顺序正确。

## 真实单股验证

- 使用平安银行 `000001` 进行一次真实受控请求，成功写入121条，日期范围为2026-03-03至2026-08-25。
- 2026-08-25已有 `EAST_MONEY` 正式记录未被覆盖；2026-08-24及更早记录保存为 `EAST_MONEY_HISTORY`。
- 单股验证后数据库新增120条历史记录；全量后台任务未启动。

## 验证

- 新增资金流向回补固定样本测试4个，与原日线回补测试合计8个全部通过。
- Maven完整测试通过：107个测试，失败0，错误0。
- 脚本命令行参数、交易日范围解析、沪深代码转换、13字段结构校验和日期过滤已验证。

## 后续执行

- 东方财富历史接口需要每只股票先访问对应个股页面预热，再等待1.5秒请求JSON；全量沪深A股5211只预计约4小时20分钟，网络异常退避时间不计入估算。
- 全量执行前保留人工确认；执行过程中通过 `target` 日志和缓存目录观察进度，出现403或429立即停止。
