# 上证指数分钟缺口补录

## 问题

2026-09-04 盘中应用被意外关闭后，`index_minute_quote` 缺少上证指数 `14:44` 分钟记录。补录前，当日从09:31至14:50应有230个分钟时点，数据库实际有229个，唯一缺口为14:44。

## 处理

- 从腾讯当日上证指数分时接口读取真实14:44记录，未使用相邻价格插值。
- 腾讯返回的14:44价格为 `3921.73`，源数据日期为 `20260904`。
- 相邻数据库记录为14:43 `3921.65`、14:45 `3918.74`，昨收均为 `3942.09`。
- 仅向 `index_minute_quote` 幂等补录 `000001 / 2026-09-04 14:44` 一条记录，数据源标记为 `TENCENT`。

## 验证

- 补录影响行数：1。
- 补录后，从09:31至14:50的230个预期分钟时点全部存在，缺失时点为0。
- 本次只修复数据库中的真实分钟数据，没有修改业务代码和表结构，因此未运行Maven测试。

## 后续限制

当前分钟采集仍不支持应用停机后的自动补录；再次发生停机时，仍需使用真实行情源核对并补齐缺口。
