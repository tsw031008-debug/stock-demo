# 固定股票数量完整性阈值

日期：2026-08-21  
状态：已完成

## 完成内容

- 股票基础信息采集和股票日行情采集的最小股票数量统一固定为3000。
- 两个采集编排类均使用类内常量 `MINIMUM_STOCK_COUNT`，不再通过构造器读取配置。
- 删除 `stock.basic.sync.minimum-stock-count` 及环境变量 `STOCK_BASIC_MINIMUM_STOCK_COUNT` 的配置入口。
- 更新相关测试：2999只股票时拒绝同步，3000只股票时允许同步。

## 验证

- 两个相关测试类共9个测试全部通过。
- Maven完整测试54个全部通过，失败0、错误0。

