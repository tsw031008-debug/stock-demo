# 默认子板块北交所代码迁移

## 问题

功能8真实查询在周期类成员 `871634` 处触发数据完整性异常。股票基础信息和股票日线均没有该代码，但同一交易日已经采集北交所920号段股票，说明问题来自V12默认子板块保留了代码切换前的旧证券代码。

## 处理

- 按北京证券交易所新旧代码对照表核对V12中的北交所成员，共发现21个旧代码。
- 新增V13迁移，仅更新 `stock_custom_plate_member.stock_code`，将21个旧代码替换为对应920号段代码。
- 不修改V12，保证已经应用过V12的数据库可以通过后续迁移正常升级。
- 不修改 `stock_basic` 和 `stock_daily_quote`，这两张表当前已经按920号段采集。
- 保持默认成员关系数量为1084，不改变成员所属子板块和启用状态。

## 验证

- Mapper集成测试依次执行V11、V12、V13。
- 断言默认成员总数为1084、21个旧代码全部消失、21个对应920代码全部存在。
- 定向测试通过：1个测试，失败0、错误0、跳过0。
- 完整Maven测试通过：193个测试，失败0、错误0、跳过0。

## 本地复测

1. 应用V13迁移。
2. 查询 `stock_custom_plate_member`，确认旧代码数量为0、新代码数量为21。
3. 重新调用 `GET /api/plate/turnoverComparison`，确认不再因旧北交所代码缺少基础信息或日线而失败。
