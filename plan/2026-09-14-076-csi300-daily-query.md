# 沪深300日行情查询接口

日期：2026-09-14

- 新增 GET /api/indexStyle/csi300?tradeDate=yyyy-MM-dd，查询指定交易日已落库的沪深300日行情。
- 返回 indexCode、indexName、tradeDate、closePrice、previousClosePrice；价格单位为指数点位，日期固定 yyyy-MM-dd。
- 复用 index_daily_quote 和 IndexDailyQuoteMapper.selectByTradeDatesAndCodes，不新增表或 SQL。
- 缺日期、非法日期、非交易日或缺少行情时明确提示；不回退查询日期，不触发网络采集。
- 本次仅完成数据库查询。现有四指数采集不包含沪深300，后续需补充采集及历史数据，功能15统计尚未实现。
- 定向测试9项通过；全量测试354项通过，失败0、错误0、跳过0。

后续变更：用户取消此查询接口，已在077记录中删除接口及专用代码，改为扩展每日自动采集。
